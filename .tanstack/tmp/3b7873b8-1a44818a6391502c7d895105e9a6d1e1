import { createFileRoute, Link } from "@tanstack/react-router";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export const Route = createFileRoute("/games/")({
  component: GamesPage,
});

function GamesPage() {
  return (
    <div className="container max-w-xl mx-auto p-6 space-y-4">
      <Card>
        <CardHeader>
          <CardTitle>🎮 Spelmeny</CardTitle>
        </CardHeader>
        <CardContent className="space-y-3">
          <Link to="/game">
            <Button className="w-full">🧠 Trivia Game</Button>
          </Link>

          <Link to="/games/cardgame">
            <Button className="w-full">🃏 Card Game</Button>
          </Link>

          <Link to="/games/pokemon-battle">
            <Button className="w-full">⚔️ Pokémon Battle</Button>
          </Link>
        </CardContent>
      </Card>
    </div>
  );
}
